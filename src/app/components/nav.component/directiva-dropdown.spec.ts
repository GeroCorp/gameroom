import { DirectivaDropdown } from './directiva-dropdown';

describe('DirectivaDropdown', () => {
  it('should create an instance', () => {
    const directive = new DirectivaDropdown();
    expect(directive).toBeTruthy();
  });
});
