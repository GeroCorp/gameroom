import { FormatearTiempoPipe } from './formatear-tiempo-pipe';

describe('FormatearTiempoPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatearTiempoPipe();
    expect(pipe).toBeTruthy();
  });
});
