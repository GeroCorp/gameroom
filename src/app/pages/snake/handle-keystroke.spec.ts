import { HandleKeystroke } from './handle-keystroke';

describe('HandleKeystroke', () => {
  it('should create an instance', () => {
    const directive = new HandleKeystroke();
    expect(directive).toBeTruthy();
  });
});
