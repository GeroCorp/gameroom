import { HandleButtons } from '../../tetris/handle-buttons';

describe('HandleButtons', () => {
  it('should create an instance', () => {
    const directive = new HandleButtons();
    expect(directive).toBeTruthy();
  });
});
